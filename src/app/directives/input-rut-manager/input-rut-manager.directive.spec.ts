import { InputRutManagerDirective } from './input-rut-manager.directive';

describe('InputRutManagerDirective', () => {
  it('should create an instance', () => {
    const directive = new InputRutManagerDirective();
    expect(directive).toBeTruthy();
  });
});
