import { NameInputDirective } from './name-input.directive';

describe('NameInputDirective', () => {
  it('should create an instance', () => {
    const directive = new NameInputDirective();
    expect(directive).toBeTruthy();
  });
});
