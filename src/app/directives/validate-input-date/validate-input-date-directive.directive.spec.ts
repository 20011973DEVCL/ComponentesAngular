import { ValidateInputDateDirectiveDirective } from './validate-input-date-directive.directive';

describe('ValidateInputDateDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateInputDateDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
