import { OnlyDigitsInputDirectiveDirective } from './only-digits-input-directive.directive';

describe('OnlyDigitsInputDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyDigitsInputDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
