import { FormatPhonePipePipe } from './format-phone-pipe.pipe';

describe('FormatPhonePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPhonePipePipe();
    expect(pipe).toBeTruthy();
  });
});
